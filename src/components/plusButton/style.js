import { StyleSheet } from "react-native";

export const container = StyleSheet.create({
  button:{
    resizeMode: "contain",
    width: 50,
    height: 50
  },
  center:{
    justifyContent: 'center',
    alignItems: 'center'
  }
})