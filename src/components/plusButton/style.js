import { StyleSheet } from "react-native";

export const container = StyleSheet.create({
  button:{
    resizeMode: "contain",
    width: 70,
    height: 70,
    bottom: 15,
    right: 15,
    position: 'absolute'
  }
})